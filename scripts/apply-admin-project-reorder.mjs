import { readFile, writeFile } from 'node:fs/promises';

const adminPagePath = 'src/pages/AdminPage.tsx';
const adminCssPath = 'src/styles/admin.css';

function replaceOnce(source, before, after, label) {
  const first = source.indexOf(before);
  if (first === -1) throw new Error(`Unable to find ${label}.`);
  if (source.indexOf(before, first + before.length) !== -1) {
    throw new Error(`Found more than one ${label}; refusing an ambiguous patch.`);
  }
  return source.slice(0, first) + after + source.slice(first + before.length);
}

let page = await readFile(adminPagePath, 'utf8');
let css = await readFile(adminCssPath, 'utf8');

page = replaceOnce(
  page,
  `  const [mediaError, setMediaError] = useState(false);\n\n  const selectedProject = useMemo(`,
  `  const [mediaError, setMediaError] = useState(false);\n  const [draggedProjectSlug, setDraggedProjectSlug] = useState<string | null>(null);\n  const [projectDropTarget, setProjectDropTarget] = useState<{\n    slug: string;\n    edge: 'before' | 'after';\n  } | null>(null);\n\n  const selectedProject = useMemo(`,
  'project reorder state anchor',
);

page = replaceOnce(
  page,
  `  function addAnalysisAsProject() {`,
  `  function renumberProjectDrafts(items: Project[]) {\n    return items.map((project, index) => ({\n      ...project,\n      number: String(index + 1).padStart(2, '0'),\n    }));\n  }\n\n  function reorderProjectDrafts(\n    sourceSlug: string,\n    targetSlug: string,\n    edge: 'before' | 'after',\n  ) {\n    if (!sourceSlug || sourceSlug === targetSlug) return;\n\n    setProjectDrafts((current) => {\n      const sourceIndex = current.findIndex((project) => project.slug === sourceSlug);\n      const targetIndex = current.findIndex((project) => project.slug === targetSlug);\n      if (sourceIndex < 0 || targetIndex < 0) return current;\n\n      const next = [...current];\n      const [moving] = next.splice(sourceIndex, 1);\n      const adjustedTargetIndex = next.findIndex((project) => project.slug === targetSlug);\n      if (adjustedTargetIndex < 0) return current;\n\n      next.splice(edge === 'after' ? adjustedTargetIndex + 1 : adjustedTargetIndex, 0, moving);\n      return renumberProjectDrafts(next);\n    });\n  }\n\n  function moveProjectByOffset(slug: string, offset: -1 | 1) {\n    setProjectDrafts((current) => {\n      const currentIndex = current.findIndex((project) => project.slug === slug);\n      const nextIndex = currentIndex + offset;\n      if (currentIndex < 0 || nextIndex < 0 || nextIndex >= current.length) return current;\n\n      const next = [...current];\n      [next[currentIndex], next[nextIndex]] = [next[nextIndex], next[currentIndex]];\n      return renumberProjectDrafts(next);\n    });\n  }\n\n  function addAnalysisAsProject() {`,
  'project reorder helpers anchor',
);

page = replaceOnce(
  page,
  `    const next = projectDrafts.filter((project) => project.slug !== selectedProject.slug);`,
  `    const next = renumberProjectDrafts(\n      projectDrafts.filter((project) => project.slug !== selectedProject.slug),\n    );`,
  'project removal numbering anchor',
);

const oldList = `          <nav className="admin-project-list" aria-label={ui.projectList}>\n            {projectDrafts.map((project) => (\n              <button\n                type="button"\n                className={project.slug === selectedSlug ? 'active' : ''}\n                key={project.slug}\n                onClick={() => setSelectedSlug(project.slug)}\n              >\n                <span>{project.number}</span>\n                <strong>{project.title}</strong>\n              </button>\n            ))}\n          </nav>`;

const newList = `          <nav\n            className={draggedProjectSlug ? 'admin-project-list is-reordering' : 'admin-project-list'}\n            aria-label={ui.projectList}\n          >\n            {projectDrafts.map((project, index) => {\n              const dropClass =\n                projectDropTarget?.slug === project.slug\n                  ? \` drop-\${projectDropTarget.edge}\`\n                  : '';\n\n              return (\n                <div\n                  className={\`admin-project-sort-item\${\n                    project.slug === selectedSlug ? ' is-active' : ''\n                  }\${draggedProjectSlug === project.slug ? ' is-dragging' : ''}\${dropClass}\`}\n                  key={project.slug}\n                  onDragOver={(event) => {\n                    if (!draggedProjectSlug || draggedProjectSlug === project.slug) return;\n                    event.preventDefault();\n                    const bounds = event.currentTarget.getBoundingClientRect();\n                    const edge = event.clientY < bounds.top + bounds.height / 2 ? 'before' : 'after';\n                    setProjectDropTarget({ slug: project.slug, edge });\n                    event.dataTransfer.dropEffect = 'move';\n                  }}\n                  onDrop={(event) => {\n                    event.preventDefault();\n                    const sourceSlug = event.dataTransfer.getData('text/plain') || draggedProjectSlug;\n                    const bounds = event.currentTarget.getBoundingClientRect();\n                    const edge = event.clientY < bounds.top + bounds.height / 2 ? 'before' : 'after';\n                    if (sourceSlug) reorderProjectDrafts(sourceSlug, project.slug, edge);\n                    setDraggedProjectSlug(null);\n                    setProjectDropTarget(null);\n                  }}\n                >\n                  <button\n                    type="button"\n                    className={\`admin-project-select\${project.slug === selectedSlug ? ' active' : ''}\`}\n                    onClick={() => setSelectedSlug(project.slug)}\n                  >\n                    <span>{String(index + 1).padStart(2, '0')}</span>\n                    <strong>{project.title}</strong>\n                  </button>\n\n                  <button\n                    type="button"\n                    className="admin-project-drag-handle"\n                    draggable\n                    aria-label={\`Drag \${project.title} to reorder\`}\n                    title="Drag to reorder"\n                    onDragStart={(event) => {\n                      setDraggedProjectSlug(project.slug);\n                      setProjectDropTarget(null);\n                      event.dataTransfer.effectAllowed = 'move';\n                      event.dataTransfer.setData('text/plain', project.slug);\n                    }}\n                    onDragEnd={() => {\n                      setDraggedProjectSlug(null);\n                      setProjectDropTarget(null);\n                    }}\n                  >\n                    <span aria-hidden="true">⠿</span>\n                  </button>\n\n                  <div className="admin-project-order-buttons">\n                    <button\n                      type="button"\n                      className="secondary admin-project-order-button"\n                      aria-label={\`Move \${project.title} up\`}\n                      disabled={index === 0}\n                      onClick={() => moveProjectByOffset(project.slug, -1)}\n                    >\n                      ↑\n                    </button>\n                    <button\n                      type="button"\n                      className="secondary admin-project-order-button"\n                      aria-label={\`Move \${project.title} down\`}\n                      disabled={index === projectDrafts.length - 1}\n                      onClick={() => moveProjectByOffset(project.slug, 1)}\n                    >\n                      ↓\n                    </button>\n                  </div>\n                </div>\n              );\n            })}\n          </nav>`;

page = replaceOnce(page, oldList, newList, 'project list markup');

const cssMarker = '/* Admin project drag ordering */';
if (!css.includes(cssMarker)) {
  css += `\n\n${cssMarker}\n.admin-project-list .admin-project-sort-item {\n  position: relative;\n  min-width: 0;\n}\n\n.admin-project-list .admin-project-select {\n  width: 100%;\n  min-height: 64px;\n  padding: 10px 86px 10px 12px;\n  display: grid;\n  grid-template-columns: 32px minmax(0, 1fr);\n  gap: 10px;\n  align-items: center;\n}\n\n.admin-project-list .admin-project-drag-handle {\n  width: 34px;\n  min-height: 30px;\n  padding: 0;\n  display: grid;\n  place-items: center;\n  position: absolute;\n  top: 7px;\n  right: 8px;\n  z-index: 3;\n  border-color: transparent;\n  background: transparent;\n  color: #7f9187;\n  cursor: grab;\n  font-size: 1.05rem;\n  line-height: 1;\n}\n\n.admin-project-list .admin-project-drag-handle:hover,\n.admin-project-list .admin-project-drag-handle:focus-visible {\n  border-color: #4a5d52;\n  background: rgba(255, 255, 255, 0.04);\n  color: #d9e4de;\n}\n\n.admin-project-list .admin-project-drag-handle:active {\n  cursor: grabbing;\n}\n\n.admin-project-sort-item.is-active .admin-project-drag-handle {\n  color: #1a2712;\n}\n\n.admin-project-sort-item.is-active .admin-project-drag-handle:hover,\n.admin-project-sort-item.is-active .admin-project-drag-handle:focus-visible {\n  border-color: rgba(13, 18, 16, 0.35);\n  background: rgba(13, 18, 16, 0.08);\n  color: #0d1210;\n}\n\n.admin-project-order-buttons {\n  display: flex;\n  gap: 4px;\n  position: absolute;\n  right: 8px;\n  bottom: 7px;\n  z-index: 3;\n}\n\n.admin-project-list .admin-project-order-button {\n  width: 30px;\n  min-height: 24px;\n  padding: 0;\n  border-radius: 6px;\n  border-color: #34483d;\n  background: rgba(255, 255, 255, 0.025);\n  color: #8fa098;\n  font-size: 0.72rem;\n  line-height: 1;\n}\n\n.admin-project-list .admin-project-order-button:hover:not(:disabled),\n.admin-project-list .admin-project-order-button:focus-visible:not(:disabled) {\n  border-color: #c7ff4a;\n  color: #c7ff4a;\n}\n\n.admin-project-sort-item.is-active .admin-project-order-button {\n  border-color: rgba(13, 18, 16, 0.24);\n  background: rgba(13, 18, 16, 0.06);\n  color: #243118;\n}\n\n.admin-project-sort-item.is-dragging {\n  opacity: 0.42;\n}\n\n.admin-project-sort-item.drop-before::before,\n.admin-project-sort-item.drop-after::after {\n  content: '';\n  position: absolute;\n  left: 4px;\n  right: 4px;\n  z-index: 6;\n  height: 2px;\n  border-radius: 999px;\n  background: #c7ff4a;\n  box-shadow: 0 0 0 3px rgba(199, 255, 74, 0.12);\n  pointer-events: none;\n}\n\n.admin-project-sort-item.drop-before::before {\n  top: -4px;\n}\n\n.admin-project-sort-item.drop-after::after {\n  bottom: -4px;\n}\n\n.admin-project-list.is-reordering .admin-project-sort-item:not(.is-dragging) .admin-project-select {\n  transition: border-color 120ms ease, transform 120ms ease;\n}\n\n@media (max-width: 620px) {\n  .admin-project-list .admin-project-select {\n    min-height: 66px;\n  }\n}\n`;
}

await writeFile(adminPagePath, page);
await writeFile(adminCssPath, css);
console.log('Admin project drag ordering applied.');
